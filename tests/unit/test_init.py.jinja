"""Test the main application entry point."""

{% if project_type == "app" %}
from main import main
{% elif project_type == "lib" %}
from {{module_name}} import hello
{% else %}
from {{module_name}} import main
{% endif %}


{% if project_type == "lib" %}
class TestHelloFunction:
    """Test cases for the library helper."""

    def test_hello_returns_greeting(self) -> None:
        """Test that hello returns the generated greeting."""
        result = hello()

        assert result == "Hello from {{project_name}}!"
{% else %}
class TestMainFunction:
    """Test cases for the main function."""

    def test_main_executes_without_error(self, capsys) -> None:
        """Test that main function runs successfully."""
        main()

        captured = capsys.readouterr()
        assert "Hello from {{project_name}}!" in captured.out
        assert "{{project_description}}" in captured.out

    def test_main_returns_none(self) -> None:
        """Test that main function returns None."""
        result = main()
        assert result is None
{% endif %}
